# Chat Capability Delta

## MODIFIED Requirements

### Requirement: Model Configuration

All backend runtime settings defined in `app.config.Settings` SHOULD be represented in env files and documentation.

`APP_NAME` MUST be configurable from environment variables and local `.env` files.
